[build]
  command = "npm run build"
  publish = "dist"

# Ensure static assets (media, logos, etc.) are served with correct headers
[[headers]]
  for = "/media/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000, immutable"
